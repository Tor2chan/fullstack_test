# Application Information
spring.application.name=test_fullstack

# Server Configuration
server.port=8080

# CORS Configuration
spring.mvc.cors.allowed-origins=http://localhost:4200
spring.mvc.cors.allowed-methods=GET,POST,PUT,DELETE

spring.mvc.cors.allowed-headers=*

# Database Configuration (
spring.datasource.url=jdbc:mysql://10.11.60.118:33306/intern?useSSL=false&serverTimezone=UTC
spring.datasource.username=sa
spring.datasource.password=P@ssw0rdR3dcr0ss

spring.jpa.show-sql=true
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Hibernate Configuration
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect


# Upload directory (use absolute path or relative to project root)
spring.web.resources.static-locations=classpath:/static/
file.upload-dir=${user.dir}/backend/db-upload/profile-pictures

# Multipart file upload properties
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB



